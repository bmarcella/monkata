export interface mylistings {
  details?: string;
  img?: string;
  description?: string;
  status?: string;
  view?: string;
  select?: string;
  oldamount?: string;
  newamount?: string;
}
export interface defaultList {
  defaultName: string;
  count: string;
  img: string;
}
export interface sortList {
  sortName: string;
}